
server:
  port: 5001

spring:
  datasource:
    # Each service should have its OWN database for microservice isolation.
    # Previously this pointed to flight_db (copy-paste error).
    url: jdbc:postgresql://localhost:5432/airline_user
    username: postgres
    password: password
